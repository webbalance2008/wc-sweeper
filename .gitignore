# Local secrets / push config — never commit
*.local.env
push-config.local.env
.env

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (only relevant if you later add a build step)
node_modules/
dist/
.wrangler/
.dev.vars
npm-debug.log*
